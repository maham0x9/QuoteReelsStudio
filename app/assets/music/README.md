# Default music

Drop royalty-free `.mp3` / `.wav` / `.m4a` files in this folder and they
will appear in the **Music** dropdown of the Video panel for every project.

Files placed here are bundled into the PyInstaller build, so they ship with
the standalone `.exe`.
